// Base URL for endpoints
export const BASE_URL = 'https://match-score.dflix.com';

// URL paths
export const URL_PATHS = {
  PREVIEW: '/preview-score/',
  LIVE: '/live-score/'
};

// Allowed actions for alerts
export const ALLOWED_ALERT_ACTIONS = [4, 6, 'WINNER', 'WICKET', 'RETIREMENT', 'NEXT_PLAYER','HALF_TIME','BATTING_INTRO','SCORE_TABLE','INNING_TABLE','TOSS','PLAYER_SUMMARY']; 